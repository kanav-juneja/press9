The component currently has no prop dependecies.
Data fetching is currently mocked. When fetching actual data, either data or the api endpoint can be passed as a prop, and so it will become a dependency.